# Anemia Cascade CDS — app runtime dependencies
# AutoGluon must match the version the frozen models were trained with.
autogluon.tabular==1.5.0

# Base-model backends required to load the WeightedEnsemble predictors.
lightgbm
catboost
xgboost
fastai

# App + data handling
streamlit>=1.30
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
joblib>=1.3
openpyxl>=3.1
